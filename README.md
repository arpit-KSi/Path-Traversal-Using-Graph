# Path-Traversal-Using-Graph
A C++-based travel planner that finds the fastest or cheapest route between cities using Dijkstra's Algorithm. It takes user-provided city and route data, processes them as a weighted graph, and outputs an optimized travel plan in HTML format.
